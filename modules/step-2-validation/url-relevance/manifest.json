{
  "id": "url-relevance",
  "name": "URL Relevance Filter",
  "description": "LLM-based URL relevance classification. Sends URL slugs, link text, and source location to an AI model to classify each as KEEP, MAYBE, or DROP for the target content type. Runs after url-dedup and url-filter to minimize token usage.",
  "version": "1.1.0",
  "step": 2,
  "category": "filtering",
  "sort_order": 5,
  "cost": "medium",
  "data_operation_default": "remove",
  "pool_precondition": "requires_items",
  "depends_on": ["url-dedup", "url-filter"],
  "usage_notes": "Run last in Step 2 to minimize LLM token spend. Haiku is sufficient — classification from URL slugs is simple. Switch to sonnet only if too many relevant pages are being dropped. Increase max_urls_per_prompt to 500 for large batches.",

  "requires_columns": ["url"],

  "options": [
    {
      "name": "ai_model",
      "type": "select",
      "label": "AI Model",
      "description": "Which model to use for classification.",
      "default": "haiku",
      "values_from": "registry.models"
    },
    {
      "name": "ai_provider",
      "type": "select",
      "label": "AI Provider",
      "description": "Which LLM provider to use.",
      "default": "anthropic",
      "values_from": "registry.providers"
    },
    {
      "name": "keep_criteria",
      "type": "textarea",
      "label": "Keep criteria",
      "presets_enabled": true,
      "description": "Comma-separated list of page types to classify as KEEP.",
      "default": "about pages, team, leadership, partnerships, integrations, products overview, services overview, awards, milestones, company news, investors, careers, pricing, contact, press releases, compliance, regulatory, case studies, interviews, expansion, acquisitions, funding, annual reports"
    },
    {
      "name": "drop_criteria",
      "type": "textarea",
      "label": "Drop criteria",
      "presets_enabled": true,
      "description": "Comma-separated list of page types to classify as DROP.",
      "default": "seasonal campaigns, single game pages, how-to-play guides, promotional offers, holiday-themed content, affiliate landing pages"
    },
    {
      "name": "confidence_threshold",
      "type": "select",
      "label": "Confidence threshold",
      "description": "How aggressively to filter uncertain URLs.",
      "default": "balanced",
      "values": ["keep_most", "balanced", "aggressive"]
    },
    {
      "name": "max_urls_per_prompt",
      "type": "number",
      "label": "Max URLs per prompt",
      "description": "Maximum number of URLs to send in a single LLM call. Larger batches use fewer API calls but may reduce accuracy.",
      "default": 200,
      "min": 10,
      "max": 500
    },
    {
      "name": "metadata_fields",
      "type": "json",
      "label": "Item metadata fields",
      "description": "Extra item fields to include in the LLM prompt alongside the URL. E.g. [\"title\", \"company\", \"location\"]. When empty, only URL slug is sent.",
      "default": []
    },
    {
      "name": "include_hostname",
      "type": "boolean",
      "label": "Show URL hostname",
      "description": "Show each URL's full host (e.g. casino.guru/news/...) instead of only the path. Lets the model tell the entity's own pages from third-party pages about other companies. Default off = today's path-only behavior.",
      "default": false
    },
    {
      "name": "entity_domain_source",
      "type": "select",
      "label": "Entity own-domain source",
      "description": "How to tell the model the entity's own website domain. 'none' = show the seed website (usually 'unknown' at step 2). 'pool_dominant_host' = derive it from the dominant host among the entity's own-crawl pool items (sitemap/page-links/browser-crawler/seed, i.e. no search/scout found_via); degrades loudly to 'unknown' if the own-crawl set is empty or ambiguous.",
      "default": "none",
      "values": ["none", "pool_dominant_host"]
    },
    {
      "name": "prompt_context",
      "type": "textarea",
      "label": "Classification context",
      "presets_enabled": true,
      "description": "Framing for the LLM classifier. Describes what kind of content is being filtered and what the entity represents.",
      "default": "You are a URL relevance classifier for a company research content pipeline.\nClassify each URL based on its relevance for creating a comprehensive company profile."
    }
  ],

  "options_defaults": {
    "ai_model": "haiku",
    "ai_provider": "anthropic",
    "keep_criteria": "about pages, team, leadership, partnerships, integrations, products overview, services overview, awards, milestones, company news, investors, careers, pricing, contact, press releases, compliance, regulatory, case studies, interviews, expansion, acquisitions, funding, annual reports",
    "drop_criteria": "seasonal campaigns, single game pages, how-to-play guides, promotional offers, holiday-themed content, affiliate landing pages",
    "confidence_threshold": "balanced",
    "max_urls_per_prompt": 200,
    "metadata_fields": [],
    "include_hostname": false,
    "entity_domain_source": "none",
    "prompt_context": "You are a URL relevance classifier for a company research content pipeline.\nClassify each URL based on its relevance for creating a comprehensive company profile."
  },

  "item_key": "url",

  "output_schema": {
    "display_type": "table",
    "selectable": true,
    "flagged_when": { "relevance": ["DROP"] },
    "url": "string (required)",
    "link_text": "string",
    "source_location": "string",
    "relevance": "string",
    "entity_name": "string"
  }
}
